import React from "react";
import { Box } from "@material-ui/core";

const Games = ({ storeAndActions }) => {
  const { game } = storeAndActions.store; 

  return (
    <Box p={2}>
    </Box>
  )
}

export default Games;